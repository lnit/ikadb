window.ENV = {}
window.ENV.RailsEnv = "<%= Rails.env %>"
window.ENV.isProduction = <%= Rails.env.production? %>
window.ENV.isStaging = <%= Rails.env.staging? %>
window.ENV.isDevelopment = <%= Rails.env.development? %>
